# Gestión — raíz del repo

El código vive en **`rrhh-app/`** (Next.js). Este archivo solo existe para que el
onboarding se cargue cuando se abre una sesión en la raíz del repo en vez de
`rrhh-app/`.

**El contexto real del proyecto está en [`rrhh-app/AGENTS.md`](rrhh-app/AGENTS.md) —
leelo ANTES de tocar código.**

@./rrhh-app/AGENTS.md

Recordatorio operativo: todos los comandos (`npm run dev`, `npm run build`,
`npm test`) se corren desde `rrhh-app/`, no desde la raíz.
